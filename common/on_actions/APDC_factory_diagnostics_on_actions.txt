on_actions = {
    # Country-specific pulse: do not repeat the snapshot for every country.
    on_daily_GER = {
        effect = {
            if = {
                limit = {
                    is_ai = no
                    NOT = { has_country_flag = APDC_factory_diagnostics_disabled }
                }
                set_temp_variable = { APDC_fd_stage = 2 }
                APDC_log_factory_snapshot = yes
            }
        }
    }
}
